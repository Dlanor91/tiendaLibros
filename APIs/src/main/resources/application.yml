spring:
  application:
    name: tienda_libros

  cloud:
    config:
      enabled: false

  datasource:
    url: jdbc:mysql://localhost:3306/tienda_libros_db?createDatabaseIfNotExist=true
    username: root
    password: Administrador2025*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: stock-group
      auto-offset-reset: earliest
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: gm.events.common
        spring.json.value.default.type: gm.events.common.EventEnvelope

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

server:
  port: 8081
  servlet:
    context-path: /

ms-ventas:
  url: http://localhost:8085/api/ventas
